# Addition to A095794 â€” comment + cross-reference (not a new sequence)

> **Status:** draft. This is **not** a new submission â€” it's an
> *update* to the existing OEIS entry
> [A095794](https://oeis.org/A095794) (`a(n) = (n+1)*(3*n - 2)/2`,
> "second pentagonal numbers minus 1").

## Why we want to update A095794

A095794 begins `1, 6, 14, 25, 39, 56, 76, 99, 125, 154, ...` with
offset `1`. With a single index shift this is exactly the closed
form L_1(N) = N*(3*N âˆ’ 5)/2 that gives the first non-trivial
dimension of the Poisson algebra generated by the C(N, 2) pairwise
interaction Hamiltonians of an N-body system with a singular
pairwise potential, for N â‰¥ 3.

Specifically, A095794(N âˆ’ 1) = L_1(N) for N â‰¥ 3:

| N | L_1(N) = N*(3*N âˆ’ 5)/2 | A095794 index | A095794 value |
|---|------------------------|---------------|----------------|
| 3 | 6                      | 2             | 6              |
| 4 | 14                     | 3             | 14             |
| 5 | 25                     | 4             | 25             |
| 6 | 39                     | 5             | 39             |
| 7 | 56                     | 6             | 56             |

This connection is worth recording on A095794 because it gives the
sequence a new and physically meaningful interpretation that is
disconnected from the "pentagonal numbers minus 1" framing.

## Suggested update text (paste into A095794's Comment field)

```
Also a(N-1) = N*(3*N - 5)/2 = L_1(N) for N >= 3, the dimension of the first
level of the Lie algebra generated under the Poisson bracket by the C(N, 2)
pairwise interaction Hamiltonians H_{ij} = m_i*m_j*V(r_{ij}) of an N-body
system with a singular pairwise potential V (the value is invariant under the
choice of singular V and under the spatial dimension d in {1, 2, 3}).
- _Brian Sheppard_, Apr 21 2026
```

## Suggested cross-references (paste into A095794's `%Y` field)

```
Cf. A395423, AYYYYYY, AZZZZZZ, AWWWWWW (3-body and 4-body Poisson algebra dimension sequences from the same construction).
```

## How to submit an update

1. Go to <https://oeis.org/A095794> while logged in
2. Click the **edit** link at the top of the page
3. Add the comment to the `%C` (Comments) section, leaving any existing
   comments alone
4. Add the cross-references to the `%Y` (Cross-refs) section
5. Set status to **Proposed** when ready

The same AI-policy rule applies: **the human author must understand and
stand behind every word added.** No pasting AI text into editor query
("pink") boxes.

## Pre-flight checks

- [x] One A-number assigned (A395423 for the 3-body singular sequence)
      as of 2026-04-21 — the cross-reference now has at least one real
      A-number to point at
- [ ] Re-verify the `A095794(N âˆ’ 1) = L_1(N)` identity by direct
      substitution for N = 3, 4, 5
